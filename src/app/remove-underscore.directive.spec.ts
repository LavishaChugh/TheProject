import { RemoveUnderscoreDirective } from './remove-underscore.directive';

describe('RemoveUnderscoreDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveUnderscoreDirective();
    expect(directive).toBeTruthy();
  });
});
